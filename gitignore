# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.eggs/

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
venv/
env/
.env

# ── Local secrets — NEVER commit these ───────────────────────────────────────
# If you ever test locally, put your PAT here, not in any tracked file.
.env.local
secrets.txt
*.secret

# ── Generated output files ────────────────────────────────────────────────────
# These are rebuilt every week by the Action. No need to track history.
# Remove index.html and rejected.html from this list if you want git history.
debug.json

# ── macOS / editor noise ──────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# ── NOTE: likes.json and index.html ARE tracked intentionally ────────────────
# likes.json  → your permanent like history, committed by the Action
# index.html  → the generated digest page, served by GitHub Pages
# Do NOT add them here.
